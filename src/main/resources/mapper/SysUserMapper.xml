<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nightboot.mapper.SysUserMapper">

    <select id="findAll" resultType="com.nightboot.domain.res.sysuser.SysUserPageVo">
        select `id`,`username`,`nick_name`,`status`,`create_by`,`create_time`,`update_by`,`update_time`
        from `sys_user`
        where status in (0,1)
        <if test="q.username != null and q.username != ''">
            and `username` like concat('%',#{q.username},'%')
        </if>
        order by create_time 
    </select>

</mapper>